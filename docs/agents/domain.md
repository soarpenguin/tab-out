# Domain Docs

## Layout

Single-context: one `CONTEXT.md` + `docs/adr/` at repo root.

## Reading Order

1. Read `CONTEXT.md` at repo root for domain language and project context
2. Read `docs/adr/*.md` for architectural decisions
3. Read `AGENTS.md` for agent guide and project structure
